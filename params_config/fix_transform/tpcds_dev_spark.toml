# Inputs
queries_folder = "tmp/extended_queries_ollama_pyspark_dev"
# Outputs
destination_folder = "tmp/transformed_queries_spark_dev/"
# Parameters
max_output_size = 1
timeout_seconds = 20

[engine]
database_path = "tmp/database_parquet/TPCDS_0.1"
validator_engine = "pyspark"

[engine.spark_config]
"spark.driver.memory" = "2g"
"spark.sql.shuffle.partitions" = "4"
